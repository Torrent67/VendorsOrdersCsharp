@using Bakery.Models;

<h1>Current Vendors</h1>
<ul>

@if (Model.Count == 0)
{
  <p>You have no one in your vendors!</p>
}
@if(Model.Count != 0)
{
  @foreach (Vendors vendors in Model)
  {
    <li>@vendors.VendorName </li>
  
    <br>
  }
}

</ul>
<a href="/items/new">Add a new Vendor.</a>